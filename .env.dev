ENVIRONMENT=dev

# PostgreSQL
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=game_price
DATABASE_USER=postgres
DATABASE_PASSWORD=

# MinIO
MINIO_HOST=localhost:9000
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=

# APIs
STEAM_API_URL=https://api.steampowered.com
CHEAPSHARK_API_URL=https://www.cheapshark.com/api/1.0

# Obs: DATABASE_PASSWORD fica vazio por enquanto porque ainda não subimos o PostgreSQL.
# MinIO também fica sem chave porque ainda vamos configurar Docker.
# As URLs já podemos deixar porque são públicas.